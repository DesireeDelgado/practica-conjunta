{% extends 'base.html.twig' %}

{% block title %}Edit Plato{% endblock %}

{% block body %}
    <h1>Edit Plato</h1>

    {{ include('plato/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_plato_index') }}">back to list</a>

    {{ include('plato/_delete_form.html.twig') }}
{% endblock %}
